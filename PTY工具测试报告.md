# PTY 工具全面测试与分析报告

## 一、测试覆盖的功能

| 工具 | 测试项目 | 结果 |
|------|----------|------|
| `pty_spawn` | 基础 echo、Python 交互、Bash、长时间运行进程、空命令、超时 | ✅ 基本正常 |
| `pty_write` | 普通输入、Unicode 输入、Base64 输入、newline 参数 | ✅ 已修复 |
| `pty_read` | 全量读取、tail 模式、pattern 过滤、stripAnsi、skipEmpty | ✅ 基本正常 |
| `pty_search` | 正则搜索、简单模式搜索 | ✅ 基本正常 |
| `pty_watch` | 非持久监控 `>>>` 模式 | ✅ 正常触发 |
| `pty_match` | 事件回调机制 | ✅ 正常触发 |
| `pty_list` | 多进程状态查询、自动清理 | ✅ 正常 |
| `pty_kill` | 强制终止 + 清理 | ✅ 正常 |
| `pty_help` | 帮助文档 | ✅ 可用 |

---

## 二、发现的问题和缺点

### 🔴 严重问题（影响正常使用）

#### 1. 大量空行问题 - Buffer 膨胀 ✅ 已修复

**问题描述：** 向 Python 发送 `print()` 或循环输出，buffer 产生大量空行

**修复方案：** 在 `RingBuffer.append()` 中折叠连续换行符（`\n{2,}` → `\n`），防止缓冲区膨胀

**影响：** ✅ 已修复 - 快速消耗 buffer 空间的问题已解决

#### 2. Unicode Emoji 丢失 ✅ 已修复

**问题描述：** Emoji 字符在输出中变成空格

**修复方案：** 改进 UTF-8 解码逻辑，使用 `Buffer.toString('utf8')` 并添加跨块多字节字符的拼接处理

**影响：** ✅ 已修复 - 涉及 emoji 的场景现在可以正确显示

#### 3. Base64 中文编码问题 ✅ 已修复

**问题描述：** Base64 解码后中文显示乱码

**修复方案：** 将 `toString('binary')` 改为 `toString('utf8')`，确保中文等多字节字符正确传递

**影响：** ✅ 已修复 - Base64 + 中文多字节字符现在可以正确传递

#### 4. 大量输出在空行多的场景下难以定位 ✅ 已修复

**问题描述：** 读取结尾日志时全是空行，难以获取有意义的输出

**修复方案：** 为 `pty_read` 添加 `skipEmpty=true` 参数过滤无意义的空行

**影响：** ✅ 已修复 - 现在可以使用 `skipEmpty=true` 快速定位有效输出

大量空行掩盖了实际输出内容，需要手动搜索才能找到有效信息

---

## 三、中等问题（影响体验）

#### 5. Ctrl+C 在 Windows 上不可靠

```
pty_write(data='\x03') 发送了 ^C，但进程仍然继续运行
最终靠 timeout 超时才终止
```

`pty_help` 文档已注明此限制，但仍值得记录。在 Windows 平台上，`pty_kill` 是唯一可靠的终止方式。

#### 6. pty_read 的 offset 模式在空行多的场景效率低

```
读取最后 10 行，结果全是空行
```

**建议：** `pty_read` 应提供 `skipEmpty=true` 参数

#### 7. pty_list 不会自动清理已退出的进程

```
已退出/已死亡的进程仍然在列表中显示
```

**影响：** 长时间运行的会话中，列表会越来越长

#### 8. pty_watch 的 throttleMs 参数不够灵活

```
设置 3000ms，但在高输出场景下仍可能漏掉匹配
```

#### 9. 没有 pty_pause/pty_resume 机制

```
无法暂停 PTY 的输出捕获，也无法恢复
```

**场景：** 需要临时忽略某些输出时无法做到

---

## 四、轻微问题（用户体验）

#### 10. pty_read 不区分"空行"和"无输出"

```
offset=-10 返回 10 行，但全是空字符串
无法快速定位到有意义的输出
```

#### 11. pty_search 的 limit 参数在大量匹配时可能被忽略

```
搜索 ">>>" 返回 151 个匹配中的前 100 个
但文档没有说明匹配是按时间顺序还是其他排序
```

#### 12. pty_spawn 的 env 参数未测试环境变量传递

```
文档提到可以传入额外的环境变量，但未实际测试
```

#### 13. pty_write 的 isBase64 和 newline 组合行为不明确

```
当 isBase64=true 时，newline=true 是添加到解码后还是编码前？
从测试看是添加到解码后的内容
```

---

## 五、改进建议

### 高优先级
1. **✅ 减少空行产生** - ✅ 已修复：在 RingBuffer 中折叠连续换行符，防止缓冲区膨胀
2. **✅ 修复 Emoji/Unicode 编码** - ✅ 已修复：改进 UTF-8 解码逻辑，正确处理跨块的多字节字符包括 Emoji
3. **✅ Base64 编码修复** - ✅ 已修复：确保 Base64 解码后多字节字符正确传递

### 中优先级
4. **✅ 添加 skipEmpty 参数** - ✅ 已实现：`pty_read(skipEmpty=true)` 过滤空行
5. **✅ 自动清理已退出进程** - ✅ 已实现：`pty_list(cleanup=true)` 清理已退出的进程
6. **Windows Ctrl+C 支持** - 通过其他方式发送 Ctrl+C

### 低优先级
7. **添加 pty_pause/pty_resume** - 暂停/恢复输出捕获
8. **pty_search 排序说明** - 明确匹配的排序规则
9. **pty_spawn 超时精度** - 支持毫秒级更精细的超时控制

---

## 六、测试环境信息

- **操作系统：** Windows (Git Bash)
- **Python：** 3.12.10
- **Shell：** Git Bash
- **测试进程数：** 6 个（含已退出的）
- **总测试时长：** ~60 秒
